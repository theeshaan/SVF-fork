Tried implementing `FlowSensitive` analysis of SVF in svf-example.cpp.
Didn't work.
Reason- it tracks only top-level variables.
(My hunch - llvm IR converts lot of top-level variables into address-taken variables. Eg: p=&a will become load (addr p) (addr a). So p also has become an address-taken var. The output will contain pointees of (addr p) and (addr a) (which will be just p and a - useless info), but won't contain that p->a) :(